6.1.22 - figured out how to rotate model and copy body from the model quaternion but when fox moves according to world coordinates 
6.12.22 - new foxAnimation file to handle animations
6.19.22 - 
    Goal is link state to key press,
    if forward = walk animation
    if forward + shift = run animation
    if idle = idle animation